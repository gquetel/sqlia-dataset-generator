template,ID,description,payload_type,payload_clause
"SELECT * FROM airport WHERE icao_code = {!icao_code}",airport-S1,A select statement filtering by ICAO code that's vulnerable to SQL injection through the icao_code parameter.,string,where
"SELECT * FROM airport WHERE elevation_ft BETWEEN {elevation_ft} AND {!elevation_ft}",airport-S2,A select statement with a BETWEEN clause on elevation that's vulnerable to SQL injection through the second elevation_ft parameters.,float float,where
"SELECT * FROM airport WHERE name LIKE {!name}",airport-S3,A select statement to search airports by name vulnerable in the name field.,string,where
"SELECT * FROM airport WHERE gps_code = {!gps_code} OR local_code = {local_code}",airport-S4,A select statement filtering by GPS code or local code with a SQL injection point on the gps_code parameter.,string string,where
"SELECT id, ident, name FROM airport WHERE iso_country = {!iso_country} LIMIT {pos_number}",airport-S5,A select statement that allows SQL injection through country filter. Possess a limit clause.,string int,where
"SELECT id, ident, name FROM airport WHERE iso_country = {!iso_country}",airport-S6,A select statement that allows SQL injection through country filter.,string,where
"SELECT * FROM airport WHERE name = {!name}",airport-S7,A select statement to search airports by name vulnerable in the name field.,string,where
"SELECT * FROM airport WHERE continent = {continent} AND iso_region LIKE {!iso_region}",airport-S8,A filetering query on continent and region vulnerable on the region field.,string string,where